/**
 * Cookie操作
 * 
 * @ignore
 */
define(function (require) {
    /**
     * Cookie操作
     * 
     * @class cookie
     * @extends util
     * @singleton
     */
    var cookie = {};
    
    /**
     * 读取
     * 
     */
    cookie.get = function() {
        
    };
    
    /**
     * 写入
     * 
     */
    cookie.set = function() {
        
    };
    
    return cookie;
});
