/**
 * DOM操作
 * 
 * @ignore
 */
define(function (require) {
    /**
     * DOM操作
     * 
     * @class dom
     * @extends base
     * @singleton
     */
    var dom = {};
    
    /**
     * 获取第一个子节点
     * 
     */
    dom.first = function() {
        
    };
    
    return dom;
});
